#
# pop mail server
#
mail_server_address = november-steps.net
pop_server_port = 110
mail_server_user = admin
mail_server_password = 
mail_receive_method = pop

# if you choose true , the mail that has created ticket is deleted.
# if you choose false, the unique id of the mail that has created ticket is written in dat file.
mail_delete_enable = false

#
# trac server
#
#trac_url = http://sasakure.biz/project1/project1
#trac_user_id = 
#trac_user_password = 

#
# zabbix server
#
zabbix_url = http://172.17.1.206/redmine/
zabbix_user_id = admin
zabbix_user_password = 

#
# target mail conditions
#
target_mail_from = .+

# if you need to write space in the head of mail subject, please use %s .
target_mail_subject = inquiry:.+

#
# ticket field
#
option_fields_fix.reporter = trac incident manager

#
# mapping
# 

# Left value is trac field name . Right value is mail field name .
#mapping_priority = 重要度

# mapping_XXX_values can have multiple mapping sets.
# CAUTION : one item in mapping_XXX_values can't have multiple value.

#mapping_priority_values.blocker = 危険
#mapping_priority_values.normal = 不明
#mapping_priority_values.critical = 警告
#mapping_priority_values.minor = 情報

#mapping_component = スコープ
#mapping_type = プラグインID
#mapping_milestone = 出力日時

# mapping_XXX.parse can change time format. ${year}${month}${day}${hour}${minute}${second}

#mapping_milestone_parse = ${year}/${month}
#mapping_keywords = ノード
#mapping_version = 出力日時
#mapping_version_parse = ${year}/${month}/${day} ${hour}:${minute}:${second}
#mapping_version = original_mail_date
#mapping_version_parse = ${year}/${month}/${day} ${hour}:${minute}:${second}

# Incident Manager  mapping from zabbix mail to redmine
mapping_event_id = EVENT.ID
mapping_event_date = EVENT.DATE
mapping_node_id = NODE.ID
mapping_node_name = NODE.NAME
mapping_host_id = HOST.ID
mapping_hostname = HOSTNAME
mapping_trigger_id = TRIGGER.ID
mapping_trigger_name = TRIGGER.NAME
mapping_trigger_value = TRIGGER.VALUE
mapping_trigger_nseverity = TRIGGER.NSEVERITY
mapping_off_event_id = OFF_EVENT.ID

# Incident Manager regist chiket conf
regist_project_id = 1

error_trigger_value = 2

target_mail_title = [zabbix_mail]

off_event => 1,

custom_field_id_event_id = 5
custom_field_id_event_date = 6
custom_field_id_node_id = 7
custom_field_id_node_name = 8
custom_field_id_host_id = 9
custom_field_id_hostname = 10
custom_field_id_trigger_id = 11
custom_field_id_trigger_name = 12
custom_field_id_trigger_value = 13
custom_field_id_trigger_nseverity = 14
custom_field_id_off_event_id = 15
